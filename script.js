
// Grab the deck element
const deckEl = document.querySelector(".deck");

// Grab the card elements
const cardsArray = document.querySelectorAll(".card");

// Attach listener to deck that runs flip on click

// flip function:
// Checks which face is up (data attribute)
// Performs rotations on the front and back elements accordingly:
// face up: face rotates